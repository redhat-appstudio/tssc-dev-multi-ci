apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-${{ values.name }}-claude
  namespace: ${{ values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
