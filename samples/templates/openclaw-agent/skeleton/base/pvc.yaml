apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-${{ values.name }}-workspace
  namespace: ${{ values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
