apiVersion: v1
kind: Service
metadata:
  name: agent-${{ values.name }}
  namespace: ${{ values.namespace }}
spec:
  selector:
    agentoffice.ai/agent: ${{ values.name }}
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
